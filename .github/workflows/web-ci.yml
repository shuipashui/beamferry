name: Web receiver and sender CI

on:
  push:
    paths:
      - "app.js"
      - "index.html"
      - "sw.js"
      - "styles.css"
      - "robots.txt"
      - "sitemap.xml"
      - "web-receiver/**"
      - "shared/**"
      - "sender/**"
      - "tests/**"
      - "package.json"
      - ".github/workflows/web-ci.yml"
  pull_request:
    paths:
      - "app.js"
      - "index.html"
      - "sw.js"
      - "styles.css"
      - "robots.txt"
      - "sitemap.xml"
      - "web-receiver/**"
      - "shared/**"
      - "sender/**"
      - "tests/**"
      - "package.json"
      - ".github/workflows/web-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: web-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run protocol, receiver, FEC, and QR density tests
        run: npm test
      - name: Verify receiver entrypoints stay identical
        run: cmp --silent app.js web-receiver/app.js
      - name: Verify service worker entrypoints stay identical
        run: node --input-type=module -e 'import fs from "node:fs"; const read = (p) => fs.readFileSync(p, "utf8").replaceAll("\r\n", "\n"); if (read("sw.js") !== read("web-receiver/sw.js")) process.exit(1)'
      - name: Rebuild single-file sender
        run: npm run build:sender
      - name: Verify committed sender artifact is reproducible
        run: git diff --exit-code -- sender/dist/beamferry-sender.html
